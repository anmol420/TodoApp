import 'package:flutter/material.dart';

class CategoryTodo extends StatelessWidget {
  const CategoryTodo({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Category Todos Page',
        style: TextStyle(color: Colors.white, fontSize: 22),
      ),
    );
  }
}
